{
  "name": "@stock-flow/nest-schematics",
  "version": "1.0.0",
  "description": "Custom NestJS schematics for Stock Flow",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && mkdir -p dist/schematics/zod/template && cp src/schematics/zod/template/*.template dist/schematics/zod/template/ && cp src/schematics/zod/schema.json dist/schematics/zod/schema.json",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "tree": "bun run scripts/tree",
    "pack": "rm -f my-nest-schematics.tgz stock-flow-nest-schematics-*.tgz && bun run build && npm pack && mv stock-flow-nest-schematics-*.tgz my-nest-schematics.tgz"
  },
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@angular-devkit/core": "^22.0.6",
    "@angular-devkit/schematics": "^22.0.6",
    "@angular-devkit/schematics-cli": "^22.0.6",
    "@nestjs/schematics": "^11.1.0",
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3"
  },
  "private": "true",
  "files": [
    "dist"
  ],
  "exports": {
    ".": "./dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "directory-tree": "^3.6.0"
  },
  "schematics": "./collection.json",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts"
}
